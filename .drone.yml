build:
  image: golang
  commands:
    - cd $GOPATH
    - rm -rf *
    - wget https://raw.githubusercontent.com/couchbase/sync_gateway/master/build.sh
    - cat build.sh
    - chmod +x build.sh
    - ./build.sh
