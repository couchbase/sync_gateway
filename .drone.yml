build:
  image: golang
  commands:
    - go get
    - go build
    - go test
    - cd $GOPATH
    - rm -rf *
    - wget https://raw.githubusercontent.com/couchbase/sync_gateway/$$COMMIT/build.sh
    - cat build.sh
    - chmod +x build.sh
    - ./build.sh $$BRANCH $$COMMIT
